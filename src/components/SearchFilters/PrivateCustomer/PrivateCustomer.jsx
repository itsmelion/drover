import React from 'react';
import SearchContainer from '../SearchContainer';

const PrivateCustomer = React.memo(() => (
  <SearchContainer>
    <label>PrivateCustomer</label>
  </SearchContainer>
));

export default PrivateCustomer;
